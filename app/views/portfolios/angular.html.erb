<h1>Portfolio items</h1>

<!-- <a href="portfolios/new">Create new item</a> -->

<%= link_to "Create new item", new_portfolio_path if logged_in?(:site_admin) %>

<% @angular_portfolio_item.each do |portfolio_item| %>
<p><%= link_to portfolio_item.title, portfolio_snow_path(portfolio_item.id) %></p>
<p><%= portfolio_item.subtitle %></p>
<p><%= portfolio_item.body %></p>
<%= image_tag portfolio_item.thumb_image if !portfolio_item.thumb_image.nil? %>
<%= link_to "Edit", edit_portfolio_path(portfolio_item.id) if logged_in?(:site_admin) %>
<%= link_to  'Delete Portfolio Item', portfolio_snow_path(portfolio_item), data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'} if logged_in?(:site_admin) %>
<% end %>
